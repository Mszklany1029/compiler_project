/*| MISPLACED |*/
/* error: break expression not in while or for loop */
let
    var a : int := 3
    var b := 3
in
    for i := 0 to 10 do
        a := a + 1;
    if a > 5 then
        (break; 5)
    else
        6
end
